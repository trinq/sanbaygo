# Public assets

## Hero image placeholder

`hero.png` is a 70-byte placeholder. The source Figma URL returned only 9 bytes instead of a usable image, so replace this file when a valid export is available.
